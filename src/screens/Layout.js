import {View} from 'react-native';
import {GlobalText} from '../styles/GlobalStyle';

export default function Layout() {
  return (
    <View>
      <GlobalText style={{fontSize: 20}}>안녕하세요</GlobalText>
    </View>
  );
}
